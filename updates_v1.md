# Изменения
## Поддержка БД
Полностью переработаны репозитории, теперь они используют БД(по умолчанию по адресу localhost:5432/mydatabase2)
## Изменение сервисного слоя
Теперь он возвращает не ResponseEntiry, а сущности, которые уже обрабатывает controller
## Замена objectMapper'а
Раньше все responseEntity были типа <String>, теперь же они не фиксированны, поэтому нет необходимости передавать в responseEntity строки, можно передавать напрямую dto классы
## Переименован пакет RequestBodyGetters на более понятный dto
## Доменные классы перемещены в отдельный пакет
## Исправлен метод для скачивания файлов, теперь он передает в body ответа контент файла в виде байт
